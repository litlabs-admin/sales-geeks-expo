# Supabase SQL

Apply these files in numeric order in the Supabase Dashboard SQL Editor.

During local development, SQL is written phase by phase. SQL is canonical for the database shape; backend Drizzle schema and generated Supabase types are kept in sync with it.
